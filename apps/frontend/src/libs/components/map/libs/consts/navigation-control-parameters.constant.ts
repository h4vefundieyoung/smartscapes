import { type ControlPosition } from "mapbox-gl";

const NAVIGATION_CONTROL_PARAMETERS = {
	POSITION: "top-right" as ControlPosition,
	SHOW_COMPASS: true,
	SHOW_ZOOM: true,
	VISUALIZE_PITCH: true,
};

export { NAVIGATION_CONTROL_PARAMETERS };
