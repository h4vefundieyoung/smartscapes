import {
	type ControlPosition,
	type ScaleControlOptions,
} from "../types/types.js";

const SCALE_CONTROL_PARAMETERS = {
	MAX_WIDTH: 80,
	POSITION: "bottom-left" as ControlPosition,
	UNIT: "metric" as ScaleControlOptions["unit"],
};

export { SCALE_CONTROL_PARAMETERS };
